Start the server

python3 -m uvicorn app:app --host 0.0.0.0 --port 8000

TODO:
 * add button to progress to the next round
 * Fix forbidden bug on Results
 * add way to reload the app from an existing database
 * Add statistics on individual player performance (in admin)